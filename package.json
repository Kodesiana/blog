{
  "name": "kodesiana-hugo2",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "bun ./tool/install.ts",
    "dev": "./.bin/hugo server --buildDrafts --buildFuture",
    "preview": "./.bin/hugo server",
    "build": "./.bin/hugo --minify",
    "linkcheck": "bun ./tool/deadlinks.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "extract-urls": "^1.4.1",
    "linkinator": "^6.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.11",
    "esbuild": "^0.19.11"
  }
}
